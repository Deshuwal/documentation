import { AuditRoutingModule } from './audit.routing.module';

describe('AuditRoutingModule', () => {
  it('should create an instance', () => {
    expect(new AuditRoutingModule()).toBeTruthy();
  });
});
