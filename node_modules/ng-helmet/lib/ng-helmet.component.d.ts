import { AfterContentInit, ElementRef, OnDestroy, QueryList } from "@angular/core";
import { NgHelmetService } from "./ng-helmet.service";
export declare class NgHelmetComponent implements AfterContentInit, OnDestroy {
    private readonly el;
    private readonly ngHelmetService;
    constructor(el: ElementRef<HTMLElement>, ngHelmetService: NgHelmetService);
    private readonly id;
    readonly elId: string;
    readonly titles: QueryList<ElementRef<HTMLTitleElement>>;
    readonly metas: QueryList<ElementRef<HTMLMetaElement>>;
    private readonly destroy$;
    ngAfterContentInit(): void;
    ngOnDestroy(): void;
}
