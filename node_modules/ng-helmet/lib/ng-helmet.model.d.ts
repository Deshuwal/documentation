import { ElementRef } from "@angular/core";
import { MetaDefinition } from "@angular/platform-browser";
export declare type NgHelmet = Readonly<{
    title?: string;
    metas: MetaDefinitions;
}>;
export declare type MetaDefinitions = {
    [selector: string]: MetaDefinition;
};
export declare function buildHelmet(titleEl: ElementRef<HTMLTitleElement> | undefined, metaEls: ElementRef<HTMLMetaElement>[]): NgHelmet;
